[build]
  command   = "npm run build"
  publish   = "dist"

# Redirect all routes to index.html so React Router (if added later) works
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
